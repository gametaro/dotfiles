{{ if (eq .chezmoi.os "windows") }}

# install scoop
iwr -useb get.scoop.sh | iex

scoop bucket add versions
scoop bucket add extras
scoop install git bat fd fzf neovim-nightly nodejs ripgrep starship wezterm
# winget install git wezterm

{{ end }}
