{{ if (eq .chezmoi.os "darwin") -}}
#!/usr/bin/env bash

set -Eeuo pipefail

packages=(
  git
  curl
  fish
  gcc
  gnu-tar
  zoxide
)

brew update
brew install "${packages[@]}"
{{ end -}}
{{/* vim: set filetype=sh: */}}
